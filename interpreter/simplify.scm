(declare (unit simplify))

(declare (uses defs))

;(define simplify
;  (lambda (obj)
;    (assert (expr-object? obj) "expected expression object as first argument to simplify")
;    (letrec
;      ((f (lambda (tlist vals)
;            
;          )
;       ))
;    )
;  )
;)
